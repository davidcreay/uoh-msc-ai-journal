### Question 10

You are given a (hypothetical) package called `nnmlSVM` that provides a Support Vector Machine system. The `makeKernel` function is used to configure kernels and takes the following parameters:

* `t`: kernel type, must be `"lin"`, `"quad"`, or `"rbf"`. All other strings are invalid.
* `a0`, `a1`, `a2`, `a3`: parameters for the kernel equations; the default for all is 1.

Assume the following statements have been executed:

```r
s <- "lin";
k1 <- makeKernel(t="quad", a0=0, a1=1, a2=2, a3=3);
s <- "quad";
k2 <- makeKernel(t="lin");
k3 <- makeKernel(t=s, a3=1, a2=0, a1=1, a0=2);
k4 <- makeKernel(t="rbf", a0=1, a1=2, a2=3, a3=4);
```

Select the mathematical type of the kernels configured here:

* `k1` is a [ Select ] kernel.
* `k2` is a [ Select ] kernel.
* `k3` is a [ Select ] kernel.
* `k4` is a [ Select ] kernel.
