### Question 12

Check all statements about one-hot encoding that are correct:

* One-hot encoding uses several numeric features to encode one categorical feature.
* The sum of values in each feature constructed by one-hot encoding is 1.
* The codes for different categorical values are orthogonal.
* Applying one-hot encoding improves generalisation of supervised learning algorithms.
