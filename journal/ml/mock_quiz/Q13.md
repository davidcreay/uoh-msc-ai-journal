### Question 13

Consider the following code for optimising hyperparameters using a grid search:

```r
h1 <- 0.0;
h2 <- 0.5;
h3 <- 1.2;
bestAccuracy <- NA;

while (h1 <= 3) {
  while (h2 < 10) {
    classifier <- makeClassifier(trainingSet, h1, h2, h3);
    accuracy <- determineAccuracy(classifier, testSet);

    if (is.na(bestAccuracy) || accuracy > bestAccuracy) {
      bestAccuracy <- accuracy;
      bestH1 <- h1;
      bestH2 <- h2;
      bestH3 <- h3;
    }
    h2 <- h2 + 0.5;
  }
  h1 <- h1 + 0.1;
}
```

Which of the following statements are correct?

* `bestH3` contains an optimised setting for hyperparameter `h3`.
* `bestH1` contains an optimised setting for hyperparameter `h1`.
* Running this code multiple times will result in improved settings for `bestH1`, `bestH2` and `bestH3`.
* `bestH2` contains an optimised setting for hyperparameter `h2`.
