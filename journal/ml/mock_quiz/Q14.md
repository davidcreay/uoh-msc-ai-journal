### Question 14

Check all statements about underfitting and overfitting that are correct:

* Overfitting can be remedied by using test data for training the classifier.
* Underfitting cannot be remedied by obtaining and using more training data.
* A classifier with more hyperparameters is always more likely to overfit than a classifier with fewer hyperparameters.
* A better performance on training data than on test data indicates that a classifier is overfitting.
