### Question 9

Let $x_i$ denote the input features of a data item, $y_i$ denote the corresponding known output, and $w$ denote the normal vector of the hyperplane of separation in a linear hard-margin Support Vector Machine (SVM). Check all correct statements about support vectors below:

* Each support vector is orthogonal to all other support vectors.
* For all support vectors, $y_i(w \cdot x_i + b) = 1$.
* For all support vectors, $\alpha$ is minimal.
* The support vectors correspond to active constraints in the optimisation problem solved by the construction of the SVM.
